# Equiformer model parameters
model:
  irreps_in: "5x0e"         # 输入表示 (节点特征)
  irreps_sh: "1x0e+1x1e"    # 球谐函数表示 (边特征)
  irreps_out: "1x0e"        # 输出表示 (标量输出)
  irreps_node_attr: "5x0e"  # 节点属性表示
  irreps_edge_attr: "1x1e"  # 边属性表示
  rescale: 1.0              # 输出缩放因子